// Legacy export for backward compatibility
// All new code should import from '../config/firebase'
export {
  initializeFirebase,
  getFirestore,
  getStorage,
  getAuth,
  isInitialized,
  getApp,
} from '../config/firebase';
