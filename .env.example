# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_client_id_here

# Optional: Deploy commands to a specific guild for faster testing (commands appear instantly)
# Leave commented out for global deployment (takes up to 1 hour)
# DISCORD_GUILD_ID=your_test_server_id

# Firebase Configuration
# Option 1: Service Account JSON (recommended)
# Can be base64-encoded JSON or a file path to service account JSON
# FIREBASE_SERVICE_ACCOUNT=<base64-encoded-json-or-path-to-json-file>

# Option 2: Individual credentials (alternative to FIREBASE_SERVICE_ACCOUNT)
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_CLIENT_EMAIL=your_firebase_client_email
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----\n"

# Optional: Firebase Storage Bucket
# FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Optional: Firebase Emulator Support (for local development)
# USE_FIREBASE_EMULATOR=true
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# FIREBASE_STORAGE_EMULATOR_HOST=localhost:9199

# Bot Configuration
NODE_ENV=development
